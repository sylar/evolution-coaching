.navbar
  .logo: img(src= logo)
  .logotype: img(src= logotype)
  .menu
    ul
      each item in menuItems
        li.link
          a(class= className + ' ' + item.id, href= item.url ) #{item.name}
    .switch Eng/Fr
